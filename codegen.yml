overwrite: true
schema: ${REACT_APP_GRAPHQL_ENDPOINT}
documents: "src/**/*.tsx"
generates:
  ./types.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
      maybeValue: T | undefined
  ./schema.json:
    plugins:
      - "introspection"
