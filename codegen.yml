overwrite: true
schema: ${REACT_APP_GRAPHQL_ENDPOINT}
documents: "src/**/*.tsx"
generates:
  ./types.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
    config:
      maybeValue: T | undefined
      enumsAsTypes: true
      scalars:
        Date: 'string'
        DateTime: 'string'
        UUID: 'string'
        CustomDate: 'string'
        CustomDateTime: 'string'
  ./schema.json:
    plugins:
      - "introspection"
